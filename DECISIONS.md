# AI SRE Decision Log

This file is the running log for architectural and scope decisions. Add a dated entry whenever the project makes a meaningful decision, especially if it changes or narrows the original report.

## Decisions

- 2026-07-09: Build a read-only/advisory AI SRE rather than an auto-remediation agent, because evidence-grounded investigation is impressive while autonomous infrastructure mutation is risky and unnecessary for the portfolio goal.
- 2026-07-09: Use the OpenTelemetry Astronomy Shop demo as the synthetic production environment, because it provides realistic microservices, telemetry, dashboards, load generation, and `flagd` fault injection.
- 2026-07-09: Use `flagd` fault flags to create controlled synthetic incidents, because reproducible known failures are required for demos and evaluation.
- 2026-07-09: Use LangGraph for orchestration, because incident investigation is a stateful, branching, partially parallel workflow with checkpointing and replay needs.
- 2026-07-09: Use a FastAPI webhook for alert ingestion, because Prometheus Alertmanager can call it directly and Python fits the agent ecosystem.
- 2026-07-09: Use Redis for async investigation queueing, because webhook ingestion must return quickly while investigations run in the background.
- 2026-07-09: Use Postgres as the durable state store, because investigations, findings, hypotheses, evidence, deploy history, and postmortems are relational data.
- 2026-07-09: Use pgvector inside Postgres for runbook and similar-incident retrieval, because the project does not need a separate vector database at this scale.
- 2026-07-09: Use hybrid runbook retrieval with deterministic tags plus vector ranking, because the runbook corpus is small and structured enough for tags to improve precision.
- 2026-07-09: Use Slack Bolt instead of a basic incoming webhook, because slash commands, interactive actions, threads, Block Kit, and request verification make the integration production-shaped.
- 2026-07-09: Use Pydantic for typed state, tool contracts, and structured LLM outputs, because validation is central to making the agent reliable and debuggable.
- 2026-07-09: Use LangSmith for agent tracing, because replayable graph traces are important for debugging, evaluation, and interview demos.
- 2026-07-09: Keep the reasoning model behind LangGraph model-agnostic while initially targeting OpenAI models, because this preserves flexibility while matching the chosen provider and cost profile.
- 2026-07-09: Implement commit correlation as deterministic deploy-window narrowing followed by LLM diff ranking, because pure timing correlation is too broad and pure LLM-over-all-commits is unreliable.
- 2026-07-09: Require evidence citations on every incident-brief claim, because the project must be glass-box and defensible rather than a black-box chatbot.
- 2026-07-09: Include a gate node that can stop or gather more context before conclusion, because avoiding thin-evidence conclusions is a core agentic design principle.
- 2026-07-09: Represent hypotheses with validation states and confidence scores, because incident investigation needs explicit uncertainty and invalidation, not just a single answer.
- 2026-07-09: Add a critic/evaluator step before posting final briefs, because agents tend to overrate their own conclusions.
- 2026-07-09: Treat the evaluation harness as a first-class deliverable, because measured accuracy on scripted incidents is the main differentiator from toy agent projects.
- 2026-07-09: Generate postmortems only from captured investigation evidence and event logs, because the LLM should format grounded facts rather than invent timelines.
- 2026-07-09: Defer any Go component until after the core Python MVP unless it clearly improves synthetic traffic or telemetry realism, because language diversity should not derail the main system.
- 2026-07-09: Name the project Glassbox SRE with Python package naming based on `glassbox_sre`, because the name emphasizes evidence-cited, debuggable incident investigation.
- 2026-07-09: Use a service-oriented repository layout with `apps/api`, `apps/worker`, `packages/core`, and `infra`, because the ingestion service, async investigation worker, shared contracts, and infrastructure config have distinct ownership boundaries.
- 2026-07-09: Start with a local stub notifier before wiring Slack Bolt, because the walking skeleton should prove alert ingestion, queueing, graph execution, and brief formatting before adding Slack credential and event complexity.
- 2026-07-09: Use the OpenAI API as the initial LLM provider instead of Anthropic, because LangGraph keeps the system model-agnostic and OpenAI is cheaper for this use case.
- 2026-07-09: Choose models per graph node rather than fixing one global model, because cheap classification steps and stronger reasoning steps have different cost and quality needs.
- 2026-07-09: Read the OpenAI API key from environment variables loaded through `.env` during development, because secrets must stay out of version control and deployment config should be explicit.
- 2026-07-09: Treat the OpenTelemetry demo app as an external Docker Compose dependency rather than vendoring or forking it, because the repo should own the AI SRE system and only wire against the demo environment.
- 2026-07-09: Use the standard-library `venv` module for the local Python environment, because it is universally available and avoids introducing another package manager before the project needs one.
- 2026-07-09: Use `python3` consistently in scripts and documentation, because this machine does not expose a `python` command on PATH outside the virtual environment.
- 2026-07-09: Run local Postgres from the `pgvector/pgvector:pg16` image, because it enables the vector extension while preserving normal Postgres behavior for relational state.
- 2026-07-09: Use a Redis list plus JSON-serialized Pydantic payloads for the Phase 0 alert queue, with `RPUSH` on ingestion and simple `LPOP` polling in the worker, because it is the smallest reliable queue abstraction needed to prove async ingestion without adding Celery or another worker framework.
- 2026-07-09: Store queued alert messages as `AlertmanagerWebhook.model_dump_json(by_alias=True)`, because the same shared Pydantic schema can validate both API input and worker input.
- 2026-07-09: Keep one root editable install for all internal packages instead of separate installs for `apps/api`, `apps/worker`, and `packages/core`, because a single setup command is less error-prone across repeated fresh clones.
- 2026-07-09: Explicitly map `glassbox_sre`, `glassbox_sre_api`, and `glassbox_sre_worker` in `pyproject.toml`, because implicit multi-root package discovery installed the distribution but did not expose the import packages reliably.
- 2026-07-09: Pin local setup to `pip<26` for now, because pip 26.1.2 hung during basic commands in this environment while pip 25.3 completed installs normally.
- 2026-07-09: Track the official OpenTelemetry demo as a git submodule under `infra/otel-demo/opentelemetry-demo`, because it keeps the demo external and pinned without vendoring or forking its source into this repo.
- 2026-07-09: Use a slim Glassbox SRE compose overlay on top of the official demo core compose instead of the upstream full observability overlay, because Phase 0 needs Prometheus, Alertmanager, Grafana, and Jaeger but not the heavier OpenSearch/OpAMP stack.
- 2026-07-09: Diagnose the earlier hangs as host machine disk pressure causing macOS dataless placeholder files and stuck Git/Python/Pip operations, because freeing local storage and replacing placeholder files made the existing approach work.
- 2026-07-09: Accept that `docker system prune -a` clears the local Docker image/build cache while preserving named Postgres and Redis volumes, because the stack can be re-pulled from compose without losing project data.
- 2026-07-09: Publish the OTel frontend proxy on host port `18080` while leaving the demo's internal Envoy port at `8080`, because this avoids local host port conflicts without breaking the demo load generator.
- 2026-07-09: Use `adFailure=on` as the Phase 0 real fault scenario, because the demo load generator continuously exercises `AdService/GetAds` and produces observable error spans.
- 2026-07-09: Initial Phase 0 alerting used `traces_span_metrics_calls_total`, because that metric existed in the earlier local stack before the submodule drift was discovered.
- 2026-07-09: Keep the shared Alertmanager schema minimal for Phase 0, because the real Alertmanager webhook payload is compatible with the required `status`, `alerts`, `labels`, `annotations`, and `startsAt` fields while extra fields are safely ignored.
- 2026-07-09: Move the repository remote from `krishna31102004/ai-sre` to `krishna31102004/ai-sre-new`, because the old remote had a persistent unresolved push hang and the new repo is the clean target for preserving history.
- 2026-07-09: Move the working checkout from `~/Desktop/ai-new` to `~/dev/ai-new`, because iCloud Desktop syncing corrupted tracked files and Git internals on the Desktop location, producing dataless placeholder reads and SIGBUS-style pack/fsck failures; the broken Desktop copy is now a quarantined backup only.
- 2026-07-09: Re-initialize `infra/otel-demo/opentelemetry-demo` as a real git submodule checkout at pinned commit `1173cfe409a7702afea20cbea48c938e1071fc18`, because the folder move left a plain copied upstream tree in place of the submodule and that drifted away from the gitlink the repo actually pins.
- 2026-07-09: Use the pinned upstream submodule `.env` file as the source of truth for OTel demo image and service versions, because that commit already ships explicit version pins including collector `ghcr.io/open-telemetry/opentelemetry-collector-releases/opentelemetry-collector-contrib:0.155.0` and using it is more reproducible than maintaining a parallel local env file.
- 2026-07-09: Keep the Glassbox collector overlay command minimal, because the repo only needs to add the extra Glassbox collector config layer and should otherwise inherit the upstream collector/image compatibility from the pinned submodule commit.
- 2026-07-09: Restore `--feature-gates=service.profilesSupport` in the Glassbox collector overlay, because the pinned upstream collector/image pair requires that feature gate for the upstream `profiles` pipeline to start successfully.
- 2026-07-09: Alert on `sum(increase(http_server_duration_milliseconds_count{service_name="frontend", http_status_code="500"}[30s])) > 0` for Phase 0, because the pinned upstream stack no longer exposes the old `traces_span_metrics_calls_total` series and the real observable symptom of `adFailure` in this version is fresh frontend HTTP 500s.
- 2026-07-09: Widen the Phase 0 alert window to `5m` and set `for: 30s`, because the observed fault rate is about one frontend 500 every 35 seconds but the series is bursty enough that `2m` and `4m` windows still missed the fault; `5m` is the shortest lookback that reliably fired in the live stack, with the trade-off that resolution takes about five minutes after the fault stops.
- 2026-07-09: Override the demo submodule's floating `DEMO_VERSION=latest` from the parent Glassbox overlay with `DEMO_VERSION=2.2.0`, because the pinned upstream commit already targets that release family and the local stack should not drift on floating tags after the collector version mismatch incident.
- 2026-07-09: Revert the attempted `DEMO_VERSION=2.2.0` runtime pin after the registry rejected `2.2.0-*` demo service tags, because the upstream demo image family is published only under `latest-*` aliases in this environment; the reproducible pin remains the submodule git commit plus the explicit collector/infra image versions already recorded elsewhere.
- 2026-07-09: Start Phase 1 with a minimal LangGraph `triage -> brief` workflow wired into the existing worker notifier path, because it introduces graph execution without changing the proven Phase 0 ingestion, Redis queue, polling, or console output path.
- 2026-07-09: Use `gpt-4.1-mini` as the initial OpenAI model for the triage node, because the node only extracts alert metadata and should stay cost-efficient until deeper reasoning nodes are added.
- 2026-07-09: Treat LangSmith tracing as optional in local development, because the graph should run with OpenAI alone and only emit traces when `LANGSMITH_*` environment variables are configured.
- 2026-07-09: Require automated tests for every Phase 1+ checkpoint and mark live-infrastructure or real-API tests with `@pytest.mark.live`, because default test runs should be fast and deterministic while still allowing explicit end-to-end verification.
- 2026-07-09: Verify the Phase 1 LangGraph walking skeleton against the real `adFailure` alert with one successful OpenAI call, using `gpt-4.1-mini` with 317 input tokens, 53 output tokens, and 370 total tokens for the firing alert triage.
- 2026-07-09: Store deploy history as typed deployment rows keyed by `deployment_id` with service, environment, deploy time, commit SHA, title, and repo path, because commit correlation needs deterministic deploy-window narrowing before any LLM ranking.
- 2026-07-09: Keep seeded Phase 1 scenario files under the repository-owned `scenarios/` tree and never inside `infra/otel-demo/opentelemetry-demo`, because the OTel demo submodule must stay pinned and unmodified.
- 2026-07-09: Use three real seeded commits for the first correlation scenario: one frontend/ad ground-truth commit and shipping/currency distractors, because `git log` and `git show` evidence is more credible than a purely synthetic in-memory fixture.
- 2026-07-09: Move the Glassbox Postgres host port to `15432`, because this machine has a local Postgres on `localhost:5432` and Phase 1 persistence needs an unambiguous connection to the Docker Postgres service.
- 2026-07-09: Extend the Phase 1 graph to `triage -> commit_correlation -> brief`, with commit correlation as the first investigator node, because this preserves the eventual fan-out architecture while keeping only one implemented investigator in Phase 1.
- 2026-07-09: Persist each investigation and commit-correlation finding to Postgres during worker execution, because Phase 1 results need durable audit records rather than console-only output.
- 2026-07-09: Use deterministic deploy-window, service, path, and diff extraction before LLM ranking, because the LLM should rank a narrowed evidence set rather than search the entire Git history.
- 2026-07-09: Use OpenAI function-calling structured output for commit-correlation findings, because strict JSON-schema response format rejected the flexible evidence metadata shape while function calling still returns validated Pydantic objects.
- 2026-07-09: Verify the first full Phase 1 commit-correlation run against the real `adFailure` alert, ranking `41080eb518884c6aeede13111f8214a7c87db3fb` as the top suspect commit with confidence `0.90`; the run used `gpt-4.1-mini` with 370 total triage tokens and 1,374 total commit-correlation tokens.
- 2026-07-09: Store runbooks as repo-owned Markdown under `runbooks/` with YAML-style frontmatter, because the corpus should be reviewable, versioned, and kept out of the OTel demo submodule.
- 2026-07-09: Chunk runbooks by Markdown section heading, because operational runbooks are naturally sectioned and section-level chunks produce clearer citations than arbitrary token windows at this project scale.
- 2026-07-09: Implement runbook retrieval as deterministic service/alert/symptom tag filtering followed by embedding ranking, because the corpus is small and structured enough that tag filtering should protect precision before semantic ranking.
- 2026-07-09: Use OpenAI `text-embedding-3-small` for runtime runbook embeddings and deterministic hash embeddings for fast tests, because tests must not require API calls while runtime retrieval should use real semantic vectors.
- 2026-07-09: Compute Phase 2 impact from Prometheus request counters in code, using frontend total requests, frontend HTTP 500 count, derived error rate, affected request count, and deterministic severity thresholds, because the LLM must not invent impact numbers.
- 2026-07-09: Treat latency as optional impact evidence for now, because the code should report it only when a real Prometheus query provides it rather than manufacturing a percentile from unavailable histogram data.
- 2026-07-09: Store runbook chunks and embeddings in the live Docker Postgres `runbook_chunks` and `runbook_embeddings` tables, because the retrieval pipeline needs durable, queryable corpus state and pgvector-backed similarity search for the real demo.
- 2026-07-09: Use an ivfflat pgvector index with cosine distance for runbook embeddings, because the corpus is tiny but still benefits from the same retrieval shape the production design expects.
- 2026-07-09: Add static service dependency topology plus endpoint hints in code, because `frontend -> ad` is enough to ground the Phase 2 impact estimate without inventing dynamic topology discovery.
- 2026-07-09: Expand the worker graph to fan out from triage into commit correlation, runbook retrieval, and impact estimation before a synthesis node, because Phase 2 needs parallel investigators with a single grounded brief at the end.
- 2026-07-09: Generate the final Phase 2 incident brief by deterministic synthesis of validated sub-findings, because the LLM should not invent impact numbers or citation links.
- 2026-07-09: Keep the Phase 2 test discipline project-wide with fast unit tests plus explicit live-infra checks, because every new graph node, retrieval step, and estimator needs regression coverage before the next unit of work lands.
- 2026-07-09: Verify the Phase 2 end-to-end demo against the live `adFailure` incident with a worker brief that included the suspect commit, the `otel-demo.frontend-ad-failure` runbook, and computed impact numbers; the run used `gpt-4.1-mini` for triage with 317 input tokens / 53 output tokens and 370 total, `gpt-4.1-mini` for commit correlation with 1,061 input tokens / 271 output tokens and 1,332 total, and OpenAI `text-embedding-3-small` for runbook retrieval embeddings.
- 2026-07-09: Replace Prometheus `increase()` with `max_over_time(counter) - min_over_time(counter)` for Phase 2 request counts, because `increase()` extrapolates range boundaries and produced fractional request counts such as `4.60388`; request counts are now rounded raw counter deltas and validated as integers.
- 2026-07-09: Use Postgres pgvector cosine ranking at runtime for runbook retrieval, because the incident brief's runbook score should come from the live `runbook_embeddings` table rather than an in-memory fallback over JSON embeddings.
- 2026-07-09: Introduce a shared notifier contract that returns delivery receipts while the worker continues logging the rendered brief, because the Phase 0-2 console path must remain unchanged while Slack Bolt is added later behind the same interface.
- 2026-07-09: Represent incident activity as explicit Pydantic event-log rows and postmortem models in the shared core package, because Phase 3 postmortems must be assembled from stored timestamps and evidence rather than LLM memory.
- 2026-07-09: Add Slack-related environment variables to the settings surface and `.env.example` now, because the Slack Bolt implementation will be inactive until the workspace credentials arrive but the config contract needs to be ready.
- 2026-07-10: Bind the local FastAPI service to `0.0.0.0` during Docker-backed verification, because Alertmanager reaches the macOS host through `host.docker.internal` and cannot connect to a loopback-only listener.
- 2026-07-10: Build Slack's HTTP client with the `certifi` CA bundle, because this Python installation could not validate Slack's certificate chain through its default trust store.
- 2026-07-10: Treat a resolved Alertmanager webhook as lost when the API is unavailable at that delivery instant, because Alertmanager had already transitioned the alert out of its active set and did not replay it after the API returned.
- 2026-07-10: For the OTel demo, restart `ad` only after `flagd` is healthy when refreshing fault state, because the ad JVM retained a failed DNS resolution of `flagd` after flagd restarted.
- 2026-07-10: Record stale fault application as an evaluation-environment operational risk, because long-lived demo containers may require an `ad`/`flagd` restart sequence before a changed fault flag takes effect.
- 2026-07-10: Defer worker retry handling for transient Postgres connection drops to a later resilience phase, because Phase 3 established the failure mode but should not broaden into reliability engineering work before Phase 4 evaluation.
- 2026-07-10: Generate Phase 3 postmortem content deterministically from persisted evidence and timestamps, because the first constrained LLM draft introduced unsupported facts despite an explicit grounding prompt.
- 2026-07-10: Define Phase 4 benchmark scenarios as explicit Pydantic contracts with `live_captured` versus `synthetic_replay` provenance, because evaluation metrics need stable denominators and honest source labeling before any scoring code runs.
- 2026-07-10: Seed the first Phase 4 corpus as one `live_captured` adFailure scenario plus four `synthetic_replay` payment/product-catalog scenarios with real repo commits and fixed telemetry snapshots, because this gives the evaluator five defensible cases without depending on the fragile full OTel stack for every run.
- 2026-07-10: Require each benchmark scenario deploy window to contain 4-6 candidates with same-service benign distractors, because service matching alone should not make bad-commit top-1 scoring trivially high.
- 2026-07-10: Keep Phase 4 replay adapters separate from the production worker path, because `SnapshotPrometheusClient`, fixture deploy history, and fake notification capture should support deterministic evaluation without changing the verified Phase 0-3 runtime.
- 2026-07-10: Score Phase 4 benchmark predictions from structured fields rather than narrative text, because root-cause, commit, runbook, impact, and latency metrics need deterministic comparisons and visible failures.
- 2026-07-10: Implement replay-fast as a deterministic fixture runner that writes `manifest.json`, `results.jsonl`, `summary.json`, and `summary.md`, because Phase 4 needs repeatable benchmark artifacts before any live OpenAI model-eval runs.
- 2026-07-10: Report root-cause metrics as unavailable in replay-fast mode until evaluator output exists, because scoring an unwired metric as `0.0` falsely implies the system attempted root-cause identification and failed.
- 2026-07-10: Prefer runbook alertname matches before same-service fallback, because frontend-originated product-catalog alerts need to retrieve product-catalog runbooks instead of being trapped by generic frontend runbooks.
- 2026-07-10: Use the full scenario deploy window for model-eval commit ranking instead of production service-filtered candidates, because the benchmark must measure whether the model can distinguish upstream bad commits from same-service benign distractors.
- 2026-07-10: Keep root-cause precision and recall unavailable in model-eval until a real evaluator maps model evidence to ground-truth root-cause labels, because commit SHA ranking alone is not the same metric.
- 2026-07-10: Correct two benchmark impact labels from `ticket` to `page`, because the deterministic severity rule intentionally pages when affected requests are at least 10 and the fixtures had 29 and 18 affected requests.
- 2026-07-10: Expand the Phase 4 benchmark corpus from 5 to 15 scenarios with harder same-service, upstream-service, ambiguous-runbook, and low-signal impact cases, because a five-scenario 100% model-eval result is too small to be credible.
- 2026-07-10: Remove separate root-cause precision and recall from Phase 4 summaries, because Glassbox SRE's current actionable root-cause output is the evidence-ranked suspect commit; a second root-cause label would either duplicate commit ranking or require a separate evaluator that has not been built.
- 2026-07-10: Add regression comparison artifacts for benchmark runs, because prompt, model, and ranking changes need visible metric deltas and per-scenario pass/fail flips over time.
- 2026-07-10: Defer the critic/evaluator node and README benchmark table to Phase 5 or later, because Phase 4's completed scope is the repeatable harness and honest artifacts rather than production brief gating or public documentation polish.
- 2026-07-10: Frame the public README around evidence-first, read-only incident investigation rather than autonomous remediation, because the strongest defensible claim is the verified alert-to-evidence-to-Slack/postmortem workflow in a simulated OTel environment.
- 2026-07-10: Publish both deterministic and model-eval benchmark results with explicit `live_captured` versus `synthetic_replay` corpus provenance, because the 15-scenario result is meaningful only when its controlled replay boundaries and two retained model misses are visible.
- 2026-07-10: Keep generated benchmark artifacts ignored by Git while documenting their reproducible commands and comparison paths, because run outputs are local experiment evidence rather than stable source files and should not create repository churn.
- 2026-07-10: Leave the repository unlicensed pending an explicit owner decision, because selecting an open-source license is a legal distribution choice rather than an implementation default.
- 2026-07-10: Keep benchmark-comparison tests self-contained with temporary JSON/JSONL fixtures instead of local `artifacts/` paths, because generated experiment outputs are intentionally ignored and a fresh clone must pass the default test suite without historical local state.
- 2026-07-10: Commit the OTel overlay port setting as `infra/otel-demo/glassbox.env`, because it is non-secret Compose configuration and the prior `.env.glassbox` name was silently excluded by the repository-wide secret-file ignore rule.
- 2026-07-10: Close Phase 5 with the verified documentation, demo, lint, and benchmark surfaces while explicitly deferring Slack command/UI polish, expanded tracing, remediation-proposal design, and committed runtime-output examples, because those additions would extend the project beyond its finished evidence-first advisory scope.
- 2026-07-10: Add dashboard API data through the existing FastAPI service and direct SQLAlchemy schema update path, because the project has no Alembic history and an idempotent `init_db` column check upgrades both fresh and existing databases.
- 2026-07-10: Use `glassbox:worker:heartbeat` as a Redis key with a 30-second expiry, because the worker can publish liveness every poll while the API reports a simple, bounded heartbeat age.
- 2026-07-10: Restrict the flagd proxy to `adFailure`, `paymentFailure`, and `productCatalogFailure`, because the dashboard must not expose an arbitrary flag write surface.
- 2026-07-10: Store a LangSmith URL only when a configured root trace yields one, because tracing and trace-link lookup must degrade without affecting investigation execution when credentials are absent or LangSmith is unavailable.
- 2026-07-10: Build the Phase 6 dashboard as a Vite React SPA using React Router and Tailwind CSS, because Vite keeps the client build small and direct while React Router cleanly maps the operational list, detail, status, and benchmark views.
- 2026-07-10: Serve the generated dashboard from FastAPI only after registered API routes, because a single local origin avoids CORS setup while preserving the existing webhook and `/api/*` behavior.
- 2026-07-10: Enable SQLAlchemy `pool_pre_ping` and one worker-level retry for recognized transient Postgres connection failures, because the previously deferred resilience gap caused a second worker death during Phase 6 integration; deterministic failures remain logged and discarded without retrying or stopping the polling loop.
- 2026-07-10: Close Phase 6 dashboard integration with deterministic FastAPI-to-worker-to-dashboard and LangSmith trace-persistence tests rather than another full live browser cycle, because sustained Docker pressure on the 8GB development machine caused Redis timeouts and inherited queue backlog; the browser-only demo remains documented for a fresh, clean environment.
